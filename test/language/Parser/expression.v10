#+Parser_new("STDIN", STDIN).expression().m_print(0)
#+val (thing, another) (2 + +-!*3) << 2
#+(2 + 3)
#+temp = if (var1 == var[0] || true)
#+	2
#+else
#+	other++

include "src/stdlib/stdlib.v10"
include "src/language/parser/Parser.v10"

func void main()(Parser p, Node n) {
	p = Parser_new("STDIN", STDIN)
	n = p.parse()
	n.m_print(0)
}
