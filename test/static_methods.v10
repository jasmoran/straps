include "src/stdlib/stdlib.v10"

class NodeStatic() {
	func Node new(Number num) {
		var Node obj = allocate(8).to(Node)
		obj.init(num)
		return obj
	}

	func void doStuff() {
		"Stuff done.".println()
	}
}

class Node(String message) {
	func void init(Number num) {
		this.message = num.toString()
	}

	#static func void doStuff() {
	#	"Stuff done.".println()
	#}
}

func void main()(Number ix) {
	var n = Node.new(1338)
	n.message.println()
	Node.doStuff()
}
